"""Centralised Hypothesis strategies for the catins test suite.

Phase 0 placeholder; Phase 1 populates this module with named
generators for ``proposals()``, ``rules()``, ``risk_scores()``,
``decision_systems(monoid)``, etc. Per ``../docs/PHASES.md``,
ad-hoc per-test generation is review-blocking once Phase 1 lands;
all property tests should import their generators from here.
"""
